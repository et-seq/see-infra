// The manifest is generated before dev, deploy, typecheck, and test runs.
// Add a destination file in this directory; no registry edit is required.
export { redirectDefinitions } from "./manifest.generated";
